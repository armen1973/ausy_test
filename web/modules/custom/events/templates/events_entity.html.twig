{#
/**
 * @file events_entity.html.twig
 * Default theme implementation to present Events entity data.
 *
 * This template is used when viewing Events entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_events_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('events_entity') }}>
  {% if content %}
    {{- content -}}
    fsdfsdfsfsf
  {% endif %}
</div>
